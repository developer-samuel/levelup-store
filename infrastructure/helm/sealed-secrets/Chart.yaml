apiVersion: v2
name: sealed-secrets
description: Sealed Secrets controller - encrypt secrets for GitOps
type: application
version: 0.1.0

dependencies:
  - name: sealed-secrets
    version: ">=2.15.0 <3.0.0"
    repository: https://bitnami-labs.github.io/sealed-secrets
